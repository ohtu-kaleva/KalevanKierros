<div class="col-md-8">
  <% show_latest.first(10).each do |uutiset| %>
      <h2><%= uutiset.title %></h2>

      <p>
        <%= uutiset.body.html_safe %>
      </p>
      <hr />
  <% end %>

</div>
<div class="col-lg-4">
<%= render 'latest' %>
</div>